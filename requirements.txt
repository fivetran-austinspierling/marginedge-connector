# No additional dependencies are required.
# This connector only uses `requests`, which is pre-installed in the
# Fivetran Connector SDK runtime environment - do not declare it here.
# See: https://fivetran.com/docs/connector-sdk/technical-reference#preinstalledpackages
